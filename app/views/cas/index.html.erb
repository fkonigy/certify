<h1>Certificate Authorities (CA)</h1>
<%= link_to 'New CA', new_ca_path %>

<table border="1">
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

  <% @cas.each do |ca| %>
  <tr>
    <td>
      <%= ca.name %>
    </td>
    <td>
      <%= link_to 'Show', ca_path(ca) %>
    </td>
  </tr>
  <% end %>
</table
